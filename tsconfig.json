{
  "compilerOptions": {
    /* 基础选项 */
    "target": "es6", // 指定 ECMAScript 目标版本: 'ES3' (默认), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019', 'ES2020', 或 'ESNEXT'
    "module": "commonjs", // 指定生成哪个模块系统代码: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', 或 'ESNext'
    "lib": [
      "es6",
      "dom"
    ], // 指定要包含在编译中的库文件
    "sourceMap": true, // 生成相应的 '.map' 文件
    "outDir": "./dist", // 指定输出目录的路径
    "rootDir": "./src", // 指定输入文件的根目录
    /* 严格的类型检查选项 */
    "strict": true, // 启用所有严格类型检查选项
    "noImplicitAny": true, // 在表达式和声明上有隐含的 any类型时报错
    /* 额外的检查 */
    "noUnusedLocals": true, // 有未使用的变量时，抛出错误
    "noUnusedParameters": true, // 有未使用的参数时，抛出错误
    /* 高级选项 */
    "skipLibCheck": true, // 跳过 .d.ts 类型检查文件
    "forceConsistentCasingInFileNames": true // 不允许同一个文件有不同的大小写
  },
  "include": [
    "src/**/*.ts" // TypeScript 文件位置
  ],
  "exclude": [
    "node_modules" // 忽略需要被编译的文件夹
  ]
}